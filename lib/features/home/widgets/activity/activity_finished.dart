import 'package:flutter/material.dart';

class ActivityFinishedWidget extends StatelessWidget {
  const ActivityFinishedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Čestitam, daj mob mami ili tati'),
    );
  }
}
