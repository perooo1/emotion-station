name: domain_models
publish_to: none

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  equatable: ^2.0.5
  json_annotation: ^4.8.1
  injectable: ^2.3.0
  import_sorter: ^4.6.0

dev_dependencies:
  lints: ^2.0.0
  json_serializable:
  build_runner:
