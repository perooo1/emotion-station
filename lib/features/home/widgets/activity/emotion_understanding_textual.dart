import 'package:flutter/material.dart';

class EmotionUnderstandingTextual extends StatelessWidget {
  const EmotionUnderstandingTextual({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('textual ');
  }
}
