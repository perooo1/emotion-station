import 'package:flutter/material.dart';

class EmotionRecognitionTab extends StatelessWidget {
  const EmotionRecognitionTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('recognition tab'));
  }
}
